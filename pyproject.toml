[project]
name = "fly-in"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "pygame",
	"numpy",
    "colored",
	"flake8",
	"flake8-pyproject",
	"mypy"
]

[tool.mypy]
exclude = [
	".venv"
]

[tool.flake8]
exclude = [
	".venv"
]
